// 3.9.1_drill_simple_form_letter_based_on_user_input
#include <iostream>
#include <string>
#include <std_lib_facilities.h>

int main()
{
    
    
std::string first_name;

std::cout << "Enter the name of the person you want to write to: \n";

std::cin>> first_name;
std::cout << "Dear " << first_name << ", " << std::endl ;
std::cout << "  How are you? I am fine.   " << std::endl;
std::cout << "Enter the name of your friend: \n";

std::string friend_name;
std::cin>> friend_name;
std::cout << "Have you seen " << friend_name << " lately?" << std::endl;

char friend_sex = 0;
std::cout << "Gender of your friend: 'm' for male and 'f' for female; " <<  std::endl ;
std::cin>> friend_sex;


if ( friend_sex == 'm') {
    std::cout << "If you see " << friend_name << " please ask him to call me." <<  std::endl;
    
}

else {
        if( friend_sex == 'f') {
        
         std::cout << "If you see " << friend_name << " please ask her to call me." <<  std::endl;
         
    }
    
    else {
        
        std::cout<< "Please enter 'm' for male, 'f' for female" <<  std::endl;
    }

    
}

int age =0;
std::cout << "What is the age of the person you are writing to? " << std::endl;
std::cin >> age;
std::cout << "I hear you just had a birthday and you are " << age << " years old"  << std::endl;

if ( (age <= 0) &&  (age >=110)) {
    simple_error << "You are kidding!" <<  std::endl;
    
}

else {
        if( friend_sex == 'f') {
        
         std::cout << "If you see " << friend_name << " please ask her to call me." <<  std::endl;
         
    }
    
    else {
        
        std::cout<< "Please enter 'm' for male, 'f' for female" <<  std::endl;
    }

    
}


return 0;
}
